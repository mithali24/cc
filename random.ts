function randomNumber(a: number) {
    return Math.random();
}

function randomno2(a: number): number {
    return Math.random();
}
function randomNumberadded(a: number) {
    return Math.random() * a ** 2;
}

function random(a: number, b: number): number {
    return Math.random() * (b - a) + a;
}